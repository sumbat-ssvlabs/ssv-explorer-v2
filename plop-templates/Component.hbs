import type { FC, ComponentPropsWithRef } from 'react';
import { cn } from "@/lib/utils"
{{#if addProps}}

export type {{pascalCase name}}Props = {
  // TODO: Add props or remove this type
};{{/if}}
{{#if addProps}}

type {{pascalCase name}}FC = FC<Omit<ComponentPropsWithRef<'{{componentType}}'>, keyof {{pascalCase name}}Props> & {{pascalCase name}}Props>

{{/if}}
export const {{pascalCase name}}: {{#if addProps}}{{pascalCase name}}FC{{else}}FC<ComponentPropsWithRef<'{{componentType}}'>>{{/if}} = ({ className, ...props }) => {
  return (
    <{{componentType}} className={cn(className)} {...props}>
      {{pascalCase name}}
    </{{componentType}}>
  );
};

{{pascalCase name}}.displayName = '{{pascalCase name}}';
